---
import Container from "./Container.astro";
---
<header>    
    <!-- styling start - underline when hovering - currently only underlined when clicking, not when hovering?-->
    <style>
        a:hover {
  text-decoration: underline;
}
    </style>
    <!-- styling end -->
    <nav class="absolute z-50 w-full bg-gray-900 dark:lg:bg-transparent">
        <Container>
                    <!-- 2 grid start -->
                    <div class="mt-12 grid sm:grid-cols-3 gap-4 sm:gap-12 max-w-3xl w-full mx-auto">
                        <div>
                        </div>
                        <div class="space-y-3">
                            <h2 class="text-gray-100 dark:text-white text-xl font-semibold">Beachvolley Zürich</h2>
                        </div>
                        <div class="space-y-3">
                            <h2 class="text-gray-100 dark:text-white text-xl font-semibold">Contact</h2>
                            <p class="text-gray-400 dark:text-gray-400">Constantin Quadt</p>
                            <p class="text-gray-400 dark:text-gray-400">Jonathan Beck</p>
                            <p class="text-gray-400 dark:text-gray-400">Matthias Bannert</p>
                            <p class="text-gray-400 dark:text-gray-400">hoi [at] beachvolley-zuerich.ch</p>
                        </div>
                    </div>
                    <!-- 2 grid end -->
                     <br></br>
                    <!-- is there a better way to add padding here? its the same top and bottom coincidentally, but should be done soemwhere-->

        </Container>
    </nav>
</header>
